<ContentDialog
    x:Class="Riff.UWP.Controls.DeletePlaylistConfirmationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Riff.UWP.Controls"
    xmlns:strings="using:Riff.UWP.Strings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{x:Bind strings:Resources.DeletePlaylistConfirmationDialogTitle}"
    PrimaryButtonText="{x:Bind strings:Resources.DeletePlaylistConfirmationDialogDeleteButtonText}"
    SecondaryButtonText="{x:Bind strings:Resources.DeletePlaylistConfirmationDialogCancelButtonText}"
    DefaultButton="Secondary">

    <Grid>
        <TextBlock Text="{x:Bind strings:Resources.DeletePlaylistConfirmationDialogMessage}"
                   Style="{StaticResource BaseTextBlockStyle}" />
    </Grid>
</ContentDialog>
